import 'package:flutter/cupertino.dart';

/**
 * 약속 장소 정하기 전에 필요한 정보를 입력하는 Dialog 화면
 *
 * step1 - 교통 수단 선택 화면
 * step2 - 출발지 입력 화면
 *
 * 모든 스탭 입력 시 약속 장소를 보여주기 위한 지도가 있는 화면으로 전환 가능
 */

class MeetBeforeSelectedWidget extends StatelessWidget {
  const MeetBeforeSelectedWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
